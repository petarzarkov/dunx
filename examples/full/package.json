{
  "name": "@dunx/example-full",
  "version": "0.0.0",
  "private": true,
  "description": "The dunx example app - one long-running service exercising DI, config, logging, HTTP routes, websocket gateways, guards, db, redis, files, images and OpenAPI",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "start": "bun src/main.ts",
    "test": "bun test",
    "tour": "bun src/tour.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bull-board/api": "^8.6.0",
    "@bull-board/bun": "^8.6.0",
    "@bull-board/ui": "^8.6.0",
    "@dunx/auth": "workspace:*",
    "@dunx/core": "workspace:*",
    "@dunx/dashboard": "workspace:*",
    "@dunx/http": "workspace:*",
    "@dunx/infra": "workspace:*",
    "@dunx/openapi": "workspace:*",
    "@dunx/transform": "workspace:*",
    "better-auth": "^1.6.25",
    "bullmq": "^6.0.5",
    "drizzle-orm": "^0.45.2",
    "ioredis": "^6.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@dunx/testing": "workspace:*"
  }
}

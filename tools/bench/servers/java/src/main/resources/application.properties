server.port=${PORT:0}
# One worker thread, matching every other subject in the suite. See README, "Threads".
server.tomcat.threads.max=1
server.tomcat.threads.min-spare=1
spring.main.banner-mode=off
logging.level.root=warn

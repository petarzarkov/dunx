# Independent versions

**Closed: lockstep stays.** Reopen when `@dunx/core` releases 1.0.0, at which point a
caret spans the whole major and the range policy writes itself. Reasoning in
[architecture/packaging.md](../architecture/packaging.md), "Versioning is lockstep, and that is a
correctness requirement".
